package ${wsEntity.mainPackage}.ws;

#foreach ($import in $imports)
import $import;
#end

/**
 * $wsEntity.desc
 *
 * Generated by Code-Generator on $generatedTime
 */
public class ${wsEntity.name}WsImpl implements I${wsEntity.name}Ws {

#foreach($method in $wsEntity.wsMethods)
    /**
     * $method.desc
     *
     * @param inDto
     * @return
     */
    public $method.outDto.name $method.name#[[(]]#${method.inDto.name} inDto#[[)]]# {
        try {
            // TODO $method.desc
            return null;
        } catch (Exception e) {
            // TODO 系统错误处理
            return null;
        }
    }
#end

}
